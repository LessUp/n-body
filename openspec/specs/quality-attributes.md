# Capability: Quality Attributes

## Overview

Defines non-functional requirements for performance, reliability, and usability of the N-body simulation system.

---

## ADDED Requirements

### Requirement: Frame Rate Performance

The system SHALL meet specified frame rate targets for various particle counts.

#### Scenario: 100K Particles Performance
- **WHEN** simulating 100K particles
- **THEN** frame rate SHALL be at least 60 FPS

#### Scenario: 1M Particles Barnes-Hut Performance
- **WHEN** simulating 1M particles with Barnes-Hut algorithm
- **THEN** frame rate SHALL be at least 25 FPS

#### Scenario: 1M Particles Spatial Hash Performance
- **WHEN** simulating 1M particles with Spatial Hash algorithm
- **THEN** frame rate SHALL be at least 60 FPS

---

### Requirement: Memory Efficiency

The system SHALL maintain efficient memory usage.

#### Scenario: Memory per Particle
- **WHEN** 1M particles are simulated
- **THEN** total memory usage SHALL be less than 200 MB

#### Scenario: Memory Layout
- **WHEN** particle data is allocated
- **THEN** Structure of Arrays layout SHALL be used for GPU coalescing

---

### Requirement: Energy Conservation Accuracy

The system SHALL maintain energy conservation over extended simulations.

#### Scenario: Energy Drift Limit
- **WHEN** simulation runs for 1000 steps
- **THEN** energy drift SHALL be less than 1%

#### Scenario: Symplectic Integration
- **WHEN** Velocity Verlet integrator is used
- **THEN** phase space volume SHALL be preserved

---

### Requirement: Test Coverage

The system SHALL maintain comprehensive test coverage.

#### Scenario: Code Coverage
- **WHEN** tests are executed
- **THEN** coverage SHALL exceed 90% for core components

#### Scenario: Property-Based Testing
- **WHEN** property-based tests run
- **THEN** all correctness properties SHALL pass

---

### Requirement: Reliability Properties

The system SHALL satisfy defined correctness properties.

#### Scenario: Force Calculation Correctness
- **WHEN** forces are computed
- **THEN** magnitude SHALL approximate G·m₁·m₂/(r² + ε²)

#### Scenario: Barnes-Hut Tree Structure
- **WHEN** octree is constructed
- **THEN** tree SHALL contain exactly N particles and conserve mass

#### Scenario: Force Method Equivalence
- **WHEN** Barnes-Hut uses low θ
- **THEN** results SHALL converge to Direct N²

#### Scenario: CUDA-GL Data Integrity
- **WHEN** data is transferred between CUDA and OpenGL
- **THEN** values SHALL be preserved without corruption

---

### Requirement: Build Simplicity

The system SHALL provide a simple, canonical build path that also supports validation automation and editor/LSP integration.

#### Scenario: Canonical build path
- **WHEN** a contributor follows the primary build instructions
- **THEN** the repository SHALL provide one clearly documented CMake-based build path that remains consistent across docs, scripts, and automation

#### Scenario: Dependency versions are controlled
- **WHEN** the build depends on fetched tools, libraries, or actions
- **THEN** their versions SHALL be pinned or explicitly bounded in a way that is documented and reproducible

#### Scenario: LSP derives from the primary build
- **WHEN** editor tooling is configured for C++ or CUDA development
- **THEN** the recommended setup SHALL use artifacts generated by the canonical build path, including `compile_commands.json` or an equivalent compile database

---

### Requirement: API Documentation

The system SHALL provide complete API documentation.

#### Scenario: Doxygen Coverage
- **WHEN** Doxygen is run
- **THEN** all public APIs SHALL be documented

#### Scenario: Usage Examples
- **WHEN** developer needs guidance
- **THEN** at least 4 example programs SHALL be available

---

### Requirement: Documentation Completeness

All reader-facing and contributor-facing documentation SHALL be accurate, non-duplicated, and intentionally curated for this project.

#### Scenario: Canonical documents stay consistent
- **WHEN** repository documentation describes architecture, workflow, setup, or examples
- **THEN** there SHALL be one canonical version for each purpose and stale duplicates SHALL be removed or reduced to pointers

#### Scenario: Bilingual core materials stay in parity
- **WHEN** a core specification or primary onboarding document is updated
- **THEN** its required bilingual counterpart SHALL be updated to preserve parity

#### Scenario: Published links remain valid
- **WHEN** documentation references internal files, Pages routes, or GitHub surfaces
- **THEN** those links SHALL resolve to valid current targets

---

### Requirement: Validation Signal Quality

Repository automation SHALL emphasize high-signal validation that matches the real supported workflow.

#### Scenario: CI checks are meaningful
- **WHEN** a workflow runs in continuous integration
- **THEN** each required job SHALL validate a supported repository behavior rather than perform purely cosmetic or redundant checks

#### Scenario: Pages builds stay scoped
- **WHEN** a change does not affect the site or published documentation surface
- **THEN** the Pages workflow SHALL remain idle

---

### Requirement: Repository Maintainability

The repository SHALL minimize maintenance overhead by reducing configuration drift, duplicated guidance, and unnecessary tool complexity.

#### Scenario: Configuration sources agree
- **WHEN** formatting, workflow, or development instructions are defined in multiple places
- **THEN** the repository SHALL keep those sources aligned or consolidate them into a smaller canonical set

#### Scenario: Optional tooling remains lightweight
- **WHEN** a plugin, MCP integration, or auxiliary tool is considered
- **THEN** it SHALL only be adopted if it clearly improves project work without imposing disproportionate maintenance or context cost

---

## Traceability

| Requirement | Original ID | Validation Method |
|-------------|-------------|-------------------|
| Frame Rate Performance | NFR-1.1, NFR-1.2, NFR-1.3 | Performance benchmarks |
| Memory Efficiency | NFR-1.4 | Memory profiling |
| Energy Conservation Accuracy | NFR-2.1 | test_integrator.cpp |
| Test Coverage | NFR-2.2, NFR-2.3 | Coverage reports, RapidCheck |
| Reliability Properties | NFR-2.2 | Property-based tests |
| Build Simplicity | NFR-3.1 | Canonical CMake build and compile database |
| API Documentation | NFR-3.2 | Doxygen output |
| Documentation Completeness | NFR-3.3 | Docs and Pages audits |
| Validation Signal Quality | NFR-3.4 | Workflow review and CI runs |
| Repository Maintainability | NFR-3.5 | Configuration and tooling audit |
